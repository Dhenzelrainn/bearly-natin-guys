<form method="POST" action="{{ route('admin.applications.request-revision', $application['database_id']) }}" style="display: contents" onsubmit="const notes = prompt('What revisions are required?'); if (!notes) return false; this.querySelector('[name=revision_notes]').value = notes;">@csrf<input type="hidden" name="revision_notes"><button type="submit" class="button button-secondary"><i data-lucide="rotate-ccw"></i> Request revision</button></form>
<form method="POST" action="{{ route('admin.applications.reject', $application['database_id']) }}" style="display: contents" onsubmit="const reason = prompt('Reason for rejecting this application:'); if (!reason) return false; this.querySelector('[name=reason]').value = reason;">@csrf<input type="hidden" name="reason"><button type="submit" class="button button-danger-soft"><i data-lucide="circle-x"></i> Disapprove</button></form>
<form method="POST" action="{{ route('admin.applications.approve', $application['database_id']) }}" style="display: contents" onsubmit="return confirm('Approve this application?')">@csrf<button type="submit" class="button button-primary"><i data-lucide="circle-check"></i> Approve application</button></form>
